<a class="project-back" href="{{ page.back_url | relative_url }}">← {{ page.back_label }}</a>

<div class="resource-meta" aria-label="Resource details">
  <span>{{ page.pathway }}</span>
  {% if page.subject %}
    <span>{{ page.subject }}</span>
  {% elsif page.area %}
    <span>{{ page.area }}</span>
  {% endif %}
</div>

{% assign media_key = page.path | split: '/' | last | remove: '.md' %}
{% assign media = site.data.project_media[media_key] %}

{% if page.demo_link_text %}
  <section class="resource-section">
    <h2>Try the app</h2>
    <div class="project-demo-pending">
      {% if page.demo_url %}
        <a
          class="project-demo-link-text"
          href="{{ page.demo_url | relative_url }}"
          target="_blank"
          rel="noopener"
          onclick="if (window.innerWidth > 760) { var demoWindow = window.open(this.href, 'data-timetabling-demo', 'popup=yes,width=1440,height=920,resizable=yes,scrollbars=yes'); if (demoWindow) { demoWindow.opener = null; return false; } }"
        >
          {{- page.demo_link_text -}}
        </a>
      {% else %}
        <span class="project-demo-link-text" aria-disabled="true">{{ page.demo_link_text }}</span>
      {% endif %}
      {% if page.demo_note %}
        <p>{{ page.demo_note }}</p>
      {% endif %}
    </div>
  </section>
{% endif %}

<section class="resource-section">
  <h2>Question</h2>
  <p class="resource-question">{{ page.question }}</p>
</section>

<div class="project-gallery">
  {% for image in media.instructional_images %}
    <figure class="project-image project-instructional-image{% if image.wide %} project-image-wide{% endif %}">
      <img src="{{ image.src | relative_url }}" alt="{{ image.alt | escape }}" loading="lazy" decoding="async">
      <figcaption>{{ image.caption }}</figcaption>
    </figure>
  {% endfor %}
  {% unless media.source_only %}
    <figure class="project-image">
      <img
        src="{{ '/assets/img/projects/' | append: media_key | append: '.png' | relative_url }}"
        alt="{{ page.visual_caption | escape }}"
        loading="lazy"
        decoding="async"
      >
      <figcaption>{{ page.visual_caption }}</figcaption>
    </figure>
  {% endunless %}
  {% unless page.hide_source_images %}
    {% for image in media.source_images %}
      <figure class="project-image project-source-image{% if image.wide %} project-image-wide{% endif %}">
        <img src="{{ image.src | relative_url }}" alt="{{ image.alt | escape }}" loading="lazy" decoding="async">
        <figcaption>{{ image.caption }}</figcaption>
      </figure>
    {% endfor %}
  {% endunless %}
</div>

<section class="resource-section">
  <h2>Goals</h2>
  <ul>
    {% for goal in page.goals %}
      <li>{{ goal }}</li>
    {% endfor %}
  </ul>
</section>

<section class="resource-section">
  <h2>Method</h2>
  {% if page.method_intro %}
    <p>{{ page.method_intro }}</p>
  {% endif %}
  <ul>
    {% for item in page.method %}
      <li>{{ item }}</li>
    {% endfor %}
  </ul>
</section>

{% if page.core_idea %}
  <section class="resource-section">
    <h2>The core idea: adaptive backtracking</h2>
    <p>{{ page.core_idea }}</p>
    {% if page.core_steps %}
      <ol>
        {% for step in page.core_steps %}
          <li>{{ step }}</li>
        {% endfor %}
      </ol>
    {% endif %}
  </section>
{% endif %}

{% if media.examples %}
  <section class="resource-section">
    <h2>Try an example</h2>
    <div class="project-examples">
      {% for example in media.examples %}
        <article class="project-example">
          <div class="project-example-heading">
            <h3>{{ example.title }}</h3>
            <div class="project-example-actions">
              {% if example.source_url %}
                <a href="{{ example.source_url }}" target="_blank" rel="noopener">Source code ↗</a>
              {% endif %}
              <a href="{{ example.src | relative_url }}" target="_blank" rel="noopener">Open full screen ↗</a>
            </div>
          </div>
          <p>{{ example.description }}</p>
          <iframe
            class="interactive-frame"
            src="{{ example.src | relative_url }}"
            title="{{ example.title | escape }} interactive example"
            loading="lazy"
            style="aspect-ratio: {{ example.aspect_ratio }};"
            allow="fullscreen"
          ></iframe>
          <p class="example-instructions"><strong>Controls:</strong> {{ example.instructions }}</p>
        </article>
      {% endfor %}
    </div>
  </section>
{% endif %}
